import random as rnd     
import sys, re, datetime, zoneinfo
from os import listdir

import a,   b  
import c   
from d import e  
from f import g, h  

import i  j    g

from a import b as c
from d import e as f, g, h as i
from j import k aas, baas as p oop, f ish as dog, clo se as you tube


class Rect:
    
    class SubRect(object):
        
        def __init__(self, a, b):
            self.a = a
            self.b = b
    
    class_var1 = 5
    class_var2 = 6
    
    def __init__():
        self.banana = 5
        print("Yes init")
    
    def get_banana():
        return self.banana
    
    def func_with_sub_func(self, inp=5):
        def subfunc_in_classfunc(a=5, b=10):
            return a + b + 6
        
        print(inp)

def function_name(param1, param2, param3):
    def subfunc(a=5, b=6, c=7, d="Bananen音국Моя乂ㄒ$%^"):
        print(f"Hi there, input: '{a}' '{b}' '{c}' '{d}'")
        self.a = a + b + c
    
    print(param1)
    if param2 == param3:
        print("They are equal")
    
    for i in range(param1):
        if i == 5:
            print("Yes!")
    
    for j in range(param2):
        if i == 10:
            print("Yes!")

def add(a, b, sub=False):
    # Subtract.
    if sub:
        return a - b
    # Add.
    else:
        return a + b

def main():
    print(rnd.randint(0, 10))

if __name__ == "__main__":
    main()

def func_with_no_new_line(arg1=5):
    pass